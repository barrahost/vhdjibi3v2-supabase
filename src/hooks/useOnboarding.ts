// Re-export from context so existing imports continue to work
export type { TourRole } from '../contexts/OnboardingContext';
export { useOnboardingContext as useOnboarding } from '../contexts/OnboardingContext';
